# Convenience wrappers — use `uv run rdvbridge <cmd>` directly or these shortcuts

setup:
	uv run rdvbridge setup

deploy:
	uv run rdvbridge deploy

logs:
	uv run rdvbridge logs

local:
	uv run rdvbridge local
